---
import companies from '../constants/companies.json';

const currentYear = new Date().getFullYear();
const yearsSince2017 = currentYear - 2017;
---

<section
  class="m-auto mt-2 mb-6 flex h-auto max-w-4xl flex-col gap-8 p-2 text-center text-base font-bold text-white sm:text-2xl"
>
  <h2>
    I have {yearsSince2017}+ years of software engineering experience.
    <br />
     It's been an honor to work for these companies:
  </h2>
  <ul
    class="flex flex-row flex-wrap gap-y-8 items-center justify-center max-w-2xl"
  >
    {
      companies.map((item, index) => (
        <li
          class={`px-4 flex w-full sm:w-1/2 md:w-1/3 h-auto scale-100 flex-col justify-end transition hover:scale-105 ${
            index < 3 ? 'sm:w-1/3 md:w-1/3' : 'sm:w-1/2 md:w-1/2'
          }`}
        >
          <a
            class="flex w-full flex-col items-center justify-end gap-2"
            href={item.url}
            target="_blank"
          >
            <img
              class="h-auto w-1/2 sm:w-1/2 rounded"
              width="130px"
              src={item.image_url}
              alt={`${item.company} Logo`}
              title={`${item.company} Logo`}
            />
            <p class="text-sm">{item.role}</p>
            <p class="text-xs">{item.time}</p>
          </a>
        </li>
      ))
    }
  </ul>
</section>
